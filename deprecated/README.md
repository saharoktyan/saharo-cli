# Deprecated Python CLI Surface

This directory contains the deprecated Python CLI and Python SDK that were previously used by Saharo:

- `python-cli/`
- `python-sdk/`

These components are kept only as historical/deprecated source.
All active CLI development now happens in:

- `../rust-cli/`
- `../rust-sdk/`
