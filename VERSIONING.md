## Versioning

Saharo CLI uses semantic versioning (SemVer).

Compatibility with Host API is governed by protocol/version negotiation exposed by the Host API
and by a supported version range in the CLI.

